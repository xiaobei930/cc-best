# 系统架构

## 概述

<!-- 项目架构简述 -->

## 模块结构

```
项目/
├── src/                # 源代码
├── tests/              # 测试代码
├── docs/               # 文档
└── scripts/            # 脚本工具
```

## 核心模块

<!-- 描述核心模块及其职责 -->

## 数据流

```
输入 → 处理 → 输出
```

## 技术决策

| 决策 | 选择 | 原因 |
| ---- | ---- | ---- |
|      |      |      |

## 接口定义

<!-- 核心接口定义，按以下格式填写 -->

### API 端点

<!-- 示例格式：
| 端点 | 方法 | 请求体 | 响应体 | 说明 |
|------|------|--------|--------|------|
| `/api/users` | POST | `CreateUserRequest` | `User` | 创建用户 |
-->

### 数据模型

<!-- 示例格式：
```typescript
interface User {
  id: string;
  name: string;
  email: string;
  createdAt: Date;
}
```
-->

## 依赖关系

<!-- 模块间依赖关系 -->
